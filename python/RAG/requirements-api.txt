# RAG/api — aucune inférence locale, uniquement des clients HTTP vers les
# fournisseurs retenus (voir RAG/README.md, §Stack api pour le comparatif
# de prix vérifié).

cohere>=5.11.0
# Le SDK Mistral s'importe via `from mistralai.client import Mistral` (pas
# `from mistralai import Mistral`) — vérifié sur la doc officielle et le
# README PyPI actuels (version publiée : 2.10.0).
mistralai>=2.0.0
groq>=0.11.0
google-genai>=0.7.0
qdrant-client>=1.11.0
rank_bm25>=0.2.2
tiktoken>=0.8.0
PyMuPDF>=1.24.0
